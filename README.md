# Alma Digital Metadata Creator
This code creates the XML metadata required for bulk ingest of representations into Alma Digital.

The codes takes as input a Alma Analytics export in the format shown in the "template.xlsx" file, and a folder of folders each containing images of each item named with the item's barcode.

The code creates a XML file following MARC convention.
